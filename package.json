{
  "name": "iipp",
  "version": "0.1.0",
  "description": "iipp",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network hardhat"
  },
  "keywords": ["solidity", "ethereum", "nft", "web3", "smart-contract"],
  "author": "nlvle",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}